The auto generation of Makefile is obsolete. Do not use it.
It is better to use "agda -j" (currently available in the
development version of Agda only). 

There is a Makefile which takes care of that, and you don't
want to overwrite it by running the scripts in this directorry.
