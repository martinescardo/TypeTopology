This module cluster investigates compact (or searchable) types.

It is used, in particular, to establish the compactness of various
ordinals, and to show that the negation of a certain particular case
of continuity is decidable.

\begin{code}

{-# OPTIONS --without-K --exact-split --safe --auto-inline #-}

import CompactTypes
import ConvergentSequenceCompact
import ConvergentSequenceHasInf
import ADecidableQuantificationOverTheNaturals
import ExtendedSumCompact
import InfProperty
import LexicographicCompactness
import WeaklyCompactTypes
import DecidabilityOfNonContinuity

\end{code}
