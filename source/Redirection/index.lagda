Martin Escardo

This file is for taking care of direct links to github from published
papers, blogs etc. of files that have been moved to different places.

\begin{code}

{-# OPTIONS --without-K --exact-split --safe --auto-inline #-}

module Redirection.index where

import InjectiveTypes-article
import InjectiveTypes
import Redirection.Types2019

\end{code}
