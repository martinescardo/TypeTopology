\begin{code}

{-# OPTIONS --without-K --exact-split --safe --auto-inline #-}

module Groups.index where

import Groups.Groups
import Groups.FreeGroup                       -- By Marc Bezem, Thierry Coquand, Dybjer, and Martin Escardo.
import Groups.FreeGroupOfLargeLocallySmallSet -- By Marc Bezem, Thierry Coquand, Dybjer, and Martin Escardo.
import Groups.SRTclosure
\end{code}
